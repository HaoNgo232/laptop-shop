export enum UserRole {
  ADMIN = "admin",
  USER = "user",
  GUEST = "guest",
}

export enum SortOrder {
  ASC = "ASC",
  DESC = "DESC",
}

export enum AuthType {
  Bearer = "Bearer",
  None = "None",
}
